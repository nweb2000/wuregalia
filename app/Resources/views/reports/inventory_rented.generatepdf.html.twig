{% extends 'base.html.twig' %}

{% block body %}
    <h2>Inventory Items Currently Rented</h2>
    <table border="1" >
        <thead>
            <tr>
                <th>Item Id</th>
                <th>Item Description</th>
                <th>Item Type</th>
                <th>Item Color</th>
                <th>Item Donor</th>
                <th>Item School</th>
                <th>Item Discipline</th>
                <th>Item Length</th>
                <th>Item Width</th>
            </tr>
        </thead>
        <tbody>
        {% for inventory in items %}
            <tr>
                <td align="center">{{ inventory.id }}</a></td>
                <td align="center">{{ inventory.itemDescription }}</td>
                <td align="center">{{ inventory.itemType.name }}</td>
                <td align="center">{{ inventory.itemColor.name }}</td>
                <td align="center">{{ inventory.user.username }}</td>
                <td align="center">{{ inventory.itemSchool.name }}</td>
                <td align="center">{{ inventory.itemMajor.name }}</td>
                <td align="center">{{ inventory.itemLength }}</td>
                <td align="center">{{ inventory.itemWidth }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
