{% extends 'base.html.twig' %}

{% block body %}
    <h2>Report Menu:</h2>
    <table border="1" >
        <tr>
            <td> <a href="{{ path('showUsers')}}">All Users</a> </td>
            <td> <a href="{{ path('showAdmins')}}">Administrators</a> </td>
            <td> <a href="{{ path('showRegisteredUsers')}}">Registered Users</a> </td>
            <td> <a href="{{ path('showInventory')}}">All Inventory</a> </td>
            <td> <a href="{{ path('showAvailibleInventory')}}">Availible Inventory</a> </td>
            <td> <a href="{{ path('showRentedInventory')}}">Currently Rented Inventory</a> </td>
            <td> <a href="{{ path('showDonationInventory')}}">Pending Donations</a> </td>
            <td> <a href="{{ path('showSpecialRequests')}}">Special Requests</a> </td>
            <td> <a href="{{ path('showRentalHistory')}}">All Rental History</a> </td>
            <td> <a href="{{ path('showPendingArrivals')}}">Donations Pending Arrival</a> </td>
        </tr>
    </table>
    <br>
    <br>
    
    <h3>Administrators</h3>
    <table border="1" >
        <thead>
            <tr>
                <th>User Id</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Email</th>
                <th>Username</th>
                <th>Signup Date</th>
                <th>Rental History</th>
                <th>Donation History</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td align="center">{{ user.id }}</a></td>
                <td align="center">{{ user.fname }}</td>
                <td align="center">{{ user.lname }}</td>
                <td align="center">{{ user.email }}</td>
                <td align="center">{{ user.username }}</td>
                <td align="center">{% if user.signupdate %}{{ user.signupdate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td align="center"> <a href="{{ path('showUserRentalHistory', { 'userID': user.id })}}">Rentals</a> </td>
                <td align="center"> <a href="{{ path('showUserDonationHistory', { 'userID': user.id })}}">Dontations</a> </td>            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
